[project]
name = "set-prompt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
set-prompt = "set_prompt.cli:main"
